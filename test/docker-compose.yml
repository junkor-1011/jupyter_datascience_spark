version: '3'

services:
  jupyter_test:
    image: ${image:-jupyter_datascience_spark:latest}
    privileged: true
    ports:
      - "${PORT:-8888}:8888"
    restart: unless-stopped
    command: jupyter lab --ip=0.0.0.0 --port=8888 --NotebookApp.token=''
